graphId: # 图的唯一id,全局唯一。必须唯一
graphName: # 图
graphDesc: # 图定义的描述
startNode: # 指定一个图的开始节点
nodes:
  - nodeConfig:
      uniqueId: #节点id，单个图维度唯一。不写会爆炸💥
      name: # 节点名字，作用就是你不写就报错
      invokeElement: #方法级别【service_name:method_name】中间用英文符号":"分隔   类级别service_name
      desc: # 节点描述
      type: # METHOD BEAN SUBFLOW
      subFlows:
        - subFlow:
            graphId: #图Id1
            startNode: #指定开始节点
            endNode: #指定结束节点
        - subFlow:
            graphId: #图Id2
            startNode: #指定开始节点
            endNode: #指定结束节点
      next: #图结构下一个节点
      pre: #强依赖节点
      preHandler: #方法级别service_name:method_name   类级别service_name
      afterHandler: #方法级别service_name:method_name   类级别service_name